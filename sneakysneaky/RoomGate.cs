/*
 * Author: Eric Ranaldi
 * Date: 2/20/2020
 * 
 * Purpose: This script acts similarly to the navPoint script. This script gets attach to trigger colliders 
 * that the user wants to use to block the GuardAgent from passing.
 * 
 * Credits: The work is solely mine I am only adding this to ensure I do not get docked points for not having a credits section
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoomGate : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
